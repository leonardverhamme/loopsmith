interface:
  display_name: "Autonomous Deep Runs"
  short_description: "Route unattended deep workflows through Agent CLI OS and the shared runner."
  default_prompt: "Use this skill to navigate unattended deep workflows through Agent CLI OS, then choose the right workflow and worker-command path."

