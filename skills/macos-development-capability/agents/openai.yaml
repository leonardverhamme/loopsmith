interface:
  display_name: "macOS Development"
  short_description: "Route macOS build, packaging, and desktop debugging through Agent CLI OS."
  default_prompt: "Use this skill to navigate macOS work through Agent CLI OS, then switch to the right Build macOS Apps plugin skill."

