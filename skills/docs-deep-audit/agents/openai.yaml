interface:
  display_name: "Docs Deep Audit"
  short_description: "Full-repo docs audit and checklist execution"
  default_prompt: "Use $docs-deep-audit with no extra instructions to run its default full-cycle workflow: refresh the docs audit, execute the checklist in the same run, keep iterating, and only stop when complete or blocked."
  brand_color: "#6A587C"
