interface:
  display_name: "Supabase Capability"
  short_description: "Route Supabase work through Agent CLI OS with CLI-first defaults."
  default_prompt: "Use this skill to inspect the Supabase capability in Agent CLI OS, then prefer the Supabase CLI unless MCP adds real value."


