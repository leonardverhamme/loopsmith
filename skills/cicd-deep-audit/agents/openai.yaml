interface:
  display_name: "CI/CD Deep Audit"
  short_description: "Full-repo CI/CD audit and checklist execution"
  default_prompt: "Use $cicd-deep-audit with no extra instructions to run its default full-cycle workflow: refresh the CI/CD audit, execute the checklist in the same run, keep iterating, and only stop when complete or blocked."
  brand_color: "#4A6470"

