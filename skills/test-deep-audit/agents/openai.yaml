interface:
  display_name: "Test Deep Audit"
  short_description: "Full-repo test audit and checklist execution"
  default_prompt: "Use $test-deep-audit with no extra instructions to run a full-cycle repo test audit: refresh the checklist, execute it in the same run, keep iterating, and only stop when complete or blocked."
  brand_color: "#5A2A6A"

policy:
  allow_implicit_invocation: false

