interface:
  display_name: "Stripe Capability"
  short_description: "Route Stripe work through Agent CLI OS and the Stripe plugin surface."
  default_prompt: "Use this skill to inspect the Stripe capability in Agent CLI OS, then choose the right Stripe plugin route."


